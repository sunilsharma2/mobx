import 'package:flutter/material.dart';







class SellHome extends StatelessWidget {
  const SellHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("sell screen"),),);
  }
}
